<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="com.example.linteacher">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:networkSecurityConfig="@xml/network_security_config"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.Linteacher">
        <activity android:name=".ui.adminaddteacher.AddTeacherActivity"></activity>
        <activity
                android:name=".api.pojo.admin.addteacher.AddTeacherActivity"
                android:label="@string/title_activity_add_teacher"
                android:theme="@style/Theme.Linteacher.NoActionBar" />
        <activity android:name=".ui.adminedituser.AdminEditActivity" />
        <activity android:name=".ui.main.MainActivity" />
        <activity android:name=".ui.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>